-- LuaSnip plugin configuration for code snippets
return {
	"L3MON4D3/LuaSnip",
	-- follow latest release.
	version = "v2.*", -- Follow latest release
	-- install jsregexp (optional!).
	build = "make install_jsregexp", -- Install jsregexp (optional)
}
